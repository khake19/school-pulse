version: '3'

services:
  with-docker-multi-env-production:
    build:
      context: ../../
      dockerfile: docker/production/Dockerfile
    image: school-pulse-production
    ports:
      - '3003:3000'
